#include "uart.h"

int setup_uart(){return 1;}
size_t read_uart(char * buf, size_t length) {size_t xx; return xx;};
size_t write_uart(const char * buf, size_t length){return 1;}
void set_notification_uart(xTaskHandle h){};